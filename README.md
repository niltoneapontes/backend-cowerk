# backend-cowerk
Back-end da CoWerk 💎, uma aplicação web que gerencia um espaço de coworking.

O front-end desta aplicação pode ser encontrado em https://github.com/niltoneapontes/frontend-cowerk .

# Utilizando o back-end

Para utilizar esta aplicação é necessário realizar um "git clone" e em seguida rodar o comando "npm install" para instalar as dependências necessárias.
Em seguida, utilize o comando "npm start" para iniciar o servidor.

# Testando as rotas

Após iniciar o servidor, podemos utilizar aplicações como Postman e Insomnia para realizar requisições HTTP relacionadas à aplicação.

**OBS.:** Acesse os arquivos da pasta "routes" para saber quais rotas podem ser utilizadas.
